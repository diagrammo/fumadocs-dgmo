{
  "name": "fumadocs-dgmo-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build --webpack",
    "dev": "if command -v claim-port >/dev/null 2>&1; then claim-port 4344 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; next dev --webpack --port 4344",
    "start": "if command -v claim-port >/dev/null 2>&1; then claim-port 4344 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; next start --port 4344",
    "postinstall": "fumadocs-mdx"
  },
  "dependencies": {
    "@diagrammo/dgmo": "0.81.0",
    "fumadocs-core": "^16.0.0",
    "fumadocs-dgmo": "link:../..",
    "fumadocs-mdx": "^15.0.0",
    "fumadocs-ui": "^16.0.0",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remark-dgmo": "0.15.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=20.6"
  }
}
